# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets/

# Face refs are private photos (see images/refs/README.md on the image-prompt PR)
images/refs/*.jpg
images/refs/*.jpeg
images/refs/*.png
images/refs/*.webp

# Local cover renders / downloads
*.DS_Store
.idea/
.vscode/
