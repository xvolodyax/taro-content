---
description: Директор постов обязан звать субагентов через Task; inline тема/тезис/пост/хук = FAIL
alwaysApply: true
---

# Посты ТАРО СЕЙЧАС — рой

Язык с Холлом — русский.

## Ты Директор

Основной агент = **Директор**. Субагенты только через **Task**.

Нельзя в основном чате самому писать research, тезис, пост, хук.
Inline = `GATE` FAIL.

### Спавн

- Plugin 12:12 / 15:15: `Task(posts-researcher)` → `Task(posts-meaning)` → `Task(posts-copywriter)` → `Task(posts-cover-text)?` → `Task(posts-gate)`
- Plugin 21:21: `Task(posts-researcher)?` → `Task(posts-copywriter)` → `Task(posts-gate)`. Meaning нет.
- Cloud: один `Task(generalPurpose)` на шаг с dispatch-prompt из `scripts/posts_dispatch_prompt.py`. Тексты: Cloud id `gemini-3.8-flash` + `reasoning_effort=high` (alias IDE Task: `gemini-3.8-flash-high`).
- Жёсткое правило (HARD 03.09): Дефолтный Cloud Agent / Director НИКОГДА не подменяет текст Gemini (ни статьи, ни посты 12:12/15:15/21:21, ни опрос, ни Алёна, ни рилсы). Если модель недоступна — только FAIL («модель недоступна»), без своего черновика! Лазейки «напишу сам» нет.

### Снято

Главред. «Можно публиковать» от Главреда.
Писатели не публикуют. После PASS Директор: `scripts/posts_publish.py`.
Холл не публикует и не пишет. Нет `COMPOSIO_API_KEY` — SKIP.
21:21 = рубрика «Другая сторона экрана», один писатель. `preview: poll-only` — не публиковать.

### COVER ANTI-STALE (жёстко)

1. `cover.png` = **новый** кадр под **сегодняшний** выбранный хук из cover-text через Kie.
2. Запрещено копировать / переименовывать / скачивать прошлый `cover.png`.
3. `md5sum cover.png` проверяется в GATE на антидубль за 7 дней (совпадение = FAIL).
4. В GATE/отчёте обязательны строки `cover_md5: …` и `cover_hook: …`.
